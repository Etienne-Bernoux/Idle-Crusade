---
title: "feat: US 12 — Zones 4 (Cathédrale Profanée) + 5 (Enfer)"
type: feat
status: completed
date: 2026-06-16
---

# US 12 — Zones 4-5 (fin du V2 : 5 zones)

Compléter le contenu : zones 4 et 5, **pure data** (comme zone 3, US 11). Aucune logique : `spawnNextEnemy`/transition/`hasNext`/welcome-back consomment déjà les catalogues. Pas de nouveau tier (Champion reste `unlockZone 99` → prestige V3).

## Décisions
- Zone 4 = **Cathédrale Profanée**, zone 5 = **Enfer** (SPEC). Mobs/boss thématiques + bg distincts.
- Échelle **×~7 par zone** (méthodo US 10) : zone 3 ~3000 → z4 ~21000 → z5 ~150000.
- Enfer = dernière zone → loop + toast « ENFER VAINCU » (méca existante). Le boss zone 4 a maintenant `hasNext` → transition (plus de toast).

## Critères d'acceptation
- [ ] **CA1** `zones[4]` (16 vagues) + `zones[5]` (18 vagues) : 5 mobs thématiques chacune, boss nommé, bg distinct.
- [ ] **CA2** Transitions Château→Cathédrale et Cathédrale→Enfer OK (méca existante), `zonesUnlocked` progresse jusqu'à 5.
- [ ] **CA3** Enfer (dernière) → pas de zone 6 → loop + toast « 🏆 ENFER VAINCU 🏆 ».
- [ ] **CA4** Échelle cohérente (×~7) ; pas de soft-lock ; gros nombres affichés proprement (`formatNumber`).
- [ ] **CA5** Catch-up traverse z4/z5 sans écran ; save/hydrate (currentZone 4/5) OK ; desktop + mobile.

## Décisions techniques (data only, `src/App.svelte`)
- `zones[4]` Cathédrale Profanée : mobs ~16000-28000, boss Archidémon ~250000, gold ~900-1700 / 50000, bg cramoisi/violet.
- `zones[5]` Enfer : mobs ~100000-180000, boss Seigneur des Enfers ~1.8M, gold ~6500-13000 / 350000, bg lave (orange/rouge/noir).
- Rien d'autre.

## Étapes
1. Ajouter `zones[4]` + `zones[5]`. Build + `npm test` (vert, data only).
2. **Playthrough** (dps massif + load off pour blast jusqu'à l'Enfer) : transitions z3→z4→z5 OK, mobs/boss/bg distincts, toast « ENFER VAINCU » sur le boss final, pas de soft-lock, gros nombres OK. Reload conserve la zone. Mobile : pas de débordement. Revert le boost.
3. Commit (branche `claude/us-12-zones-4-5`), review, compound.

## Hors scope
- Champion (prestige V3), sprites dédiés (emojis + gradient), équilibrage fin de la courbe haute.

## Gotchas
- **Très gros nombres** (boss 1.8M) : `formatNumber` (espace fine FR) gère ; vérifier qu'ils ne débordent pas la barre PV en mobile.
- **Atteindre l'Enfer en test** : impossible à dps normal → booster massivement + désactiver le load (fresh) ; revert ensuite.
- **Échelle** : viser sans actif/relique (bonus). Le saut ×7 est volontairement un mur « recrute plus » — pas un soft-lock (on peut toujours taper).

## Sources
- SPEC : [SPEC.md](../../SPEC.md) (Zones — Cathédrale Profanée, Enfer)
- Patterns : [docs/solutions/patterns/idle-game-tick-and-popups.md](../solutions/patterns/idle-game-tick-and-popups.md) (catalogues, payoff US 11)
- Code : [src/App.svelte](../../src/App.svelte) (`zones`)
